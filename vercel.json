{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 && export PATH=\"$PATH:`pwd`/flutter/bin\" && flutter config --no-analytics && flutter doctor && flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null
}